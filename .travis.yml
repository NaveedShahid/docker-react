sudo: required
services:
  - docker 

before_install:
  - docker build -t naveedshahid/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true naveedshahid/docker-react npm run test -- --coverage